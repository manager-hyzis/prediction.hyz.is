---
title: Cancel an Order
description: Remove resting orders that have not fully filled.
icon: Ban
---

## Endpoint

`DELETE /v1/orders/{orderId}`

## Partial fills

If an order is partially filled, cancellation removes the remaining open quantity and returns unfilled collateral.

## Error handling

The API returns HTTP 404 when the order is missing and HTTP 409 if the order already filled.
